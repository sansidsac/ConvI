# ============================================================
# ConvI — Financial Banking Conversation Intelligence
# Requirements
# ============================================================

# --- Core API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# --- Speech Pipeline (DEV 2) ---
pyannote.audio>=3.1.1
faster-whisper>=1.0.1
speechbrain>=1.0.0
torch>=2.2.0
torchaudio>=2.2.0

# --- Text / NLP Pipeline (DEV 1) ---
fasttext-wheel>=0.9.2
spacy>=3.7.4
# After install run: python -m spacy download en_core_web_sm

# --- RAG Engine (DEV 3) ---
FlagEmbedding>=1.2.9          # bge-m3 embeddings
faiss-cpu>=1.8.0              # FAISS vector store (use faiss-gpu if CUDA available)
transformers>=4.40.0

# --- Local LLM (DEV 3) ---
# Qwen2.5 served via transformers or vllm (choose one)
# vllm>=0.4.0                 # Uncomment for vLLM serving
accelerate>=0.29.0

# --- Storage (DEV 4) ---
sqlalchemy>=2.0.29
psycopg2-binary>=2.9.9
alembic>=1.13.1

# --- Utilities ---
httpx>=0.27.0
aiofiles>=23.2.1
loguru>=0.7.2
tenacity>=8.2.3
