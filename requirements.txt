# ============================================================
# ConvI — Financial Banking Conversation Intelligence
# Requirements (CPU-only)
# ============================================================
# Install order matters — follow the staged install below:
#
# Stage 1 — Core API
#   pip install fastapi uvicorn[standard] pydantic pydantic-settings \
#               python-multipart python-dotenv loguru aiofiles httpx
#
# Stage 2 — PyTorch CPU + Speech Pipeline
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install pyannote.audio faster-whisper speechbrain
#
# Stage 3 — NLP
#   pip install fasttext-wheel spacy
#   python -m spacy download en_core_web_sm
#
# Stage 4 — RAG + LLM
#   pip install FlagEmbedding faiss-cpu transformers accelerate
#
# Stage 5 — Storage
#   pip install sqlalchemy psycopg2-binary alembic
# ============================================================

# --- Stage 1: Core API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
loguru>=0.7.2
aiofiles>=23.2.1
httpx>=0.27.0

# --- Stage 2: PyTorch CPU (install via --index-url, see above) ---
# torch             # CPU wheel — do NOT pip install directly from this file
# torchaudio        # CPU wheel — install via index-url command above

# Speech pipeline
pyannote.audio>=3.1.1
faster-whisper>=1.0.1
speechbrain>=1.0.0

# --- Stage 3: NLP ---
fasttext-wheel>=0.9.2
spacy>=3.7.4
# After install: python -m spacy download en_core_web_sm

# --- Stage 4: RAG + LLM ---
FlagEmbedding>=1.2.9
faiss-cpu>=1.8.0
transformers>=4.40.0
accelerate>=0.29.0

# --- Stage 5: Storage ---
sqlalchemy>=2.0.29
psycopg2-binary>=2.9.9
alembic>=1.13.1

# --- Utilities ---
tenacity>=8.2.3
